version: '2'
services:
  elasticsearch:
    build: elasticsearch
    # image: elasticsearch:5
    #volumes:
    #    - ./elasticsearch-data:/usr/share/elasticsearch/data
    ports:
        - 9200:9200
        - 9300:9300
    environment:
      - "discovery.type=single-node"
      - xpack.security.enabled=false
      - http.host=0.0.0.0
      - network.host=0.0.0.0
    expose:
      - 9300
  kibana:
    build: kibana
    # image: kibana:5
    ports:
        - 5601:5601
    links:
        - elasticsearch
    environment:
        - ELASTICSEARCH_URL=http://192.168.10.83:9200
    #    - elasticsearch.username=elastic
    #    - elasticsearch.password=elastic
        - xpack.security.enabled=false
